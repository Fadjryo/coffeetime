// app/@modal/default.js

// This component is the fallback for the @modal slot.
// It ensures the modal is HIDDEN when no interception is active.
export default function ModalDefault() {
  return null;
}
